from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Hello Devops engineer,  LenDenClub is India’s No.1 Peer-to-Peer (P2P) Lending platform which is RBI registered NBFC - P2P."

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
